[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bando-music-engine"
version = "0.1.0"
description = "Sovereign model-agnostic closed-loop music generation runtime"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
