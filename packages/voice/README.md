# Voice

Canonical consent-safe voice identity package target.

Phase 1 intentionally leaves existing voice repositories untouched. Voice backends will be consolidated behind a replaceable VoiceAdapter only after the generator + critic loop is proven.
