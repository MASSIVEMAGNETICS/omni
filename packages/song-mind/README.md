# SongMind

Canonical cognition/planning package target for the Bando music stack.

Phase 1 intentionally defines the boundary but does not migrate Song Bloom yet. The first closed-loop milestone consumes a plain JSON SongPlan; BMIR/SongMind migration follows after the adapter + critic loop passes regression.
