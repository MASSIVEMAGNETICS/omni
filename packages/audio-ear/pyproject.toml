[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bando-audio-ear"
version = "0.1.0"
description = "Sovereign audio critic and BandoRank scoring contracts"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
